# Integer-Input-Validation-Cpp
A simple C++ program to validate user input and ensure only integers are accepted. Uses cin.fail() and input buffer handling to prevent invalid entries like letters or symbols.
